@using AccountingBook.Helper;

@model List<AccountingBook.Models.AccountingBookDataListModels>

@{ 
    Layout = null;
    var i = 0;
}
<div class="row">
    <div class="col-md-12">
        <table class="table table-bordered table-hover">
            <tr>
                <th>#</th>
                <th nowrap="nowrap">類別</th>
                <th nowrap="nowrap">日期</th>
                <th>金額</th>
                <th nowrap="nowrap">備註</th>
            </tr>
            @*@for (int i = 0; i <= Model.Count() - 1; i++)
            {
                <tr>
                    <td>@(i + 1)</td>
                    <td>@(Model[i].type)</td>
                    <td>@((Model[i].Date).ToString("yyyy-MM-dd"))</td>
                    @if (i+1 == 3)
                    {                        
                        <td>@((Model[i].money.ToString("N", CultureInfo.CreateSpecificCulture("fr-FR"))))</td>
                    }
                    else
                    {
                        <td>@((Model[i].money.ToString("N0")))</td>
                    }
                    
                </tr>
            }*@
            @foreach (var m in Model)
            {
                
                <tr>
                    <td>@(++i)</td>
                    <td nowrap="nowrap">@Html.AccountingType(m.type)</td>
                    <td nowrap="nowrap">@m.Date.ToString("yyyy-MM-dd")</td>
                    <td>@m.money.ToString("#,###")</td>
                    <td nowrap="nowrap">@m.Remark</td>
                </tr>
            }
        </table>
    </div>
</div>


@*@using System.Globalization*@
@model List<AccountingBook.Models.AccountingBookDataListModels>
@{ 
    var i = 0;
}
<div class="row">
    <div class="col-md-12">
        <table class="table table-bordered table-hover">
            <tr>
                <th>#</th>
                <th>類別</th>
                <th>日期</th>
                <th>金額</th>
            </tr>
            @*@for(int i = 0; i<=Model.Count-1; i++)
            {
               <tr>
                   <td>@(i+1)</td>
                   <td>@(Model[i].type)</td>
                   <td>@(Model[i].Date.ToString("yyyy-MM-dd"))</td>
                   @if (i + 1 == 3)
                   {
                    <td>@((Model[i].money.ToString("N", CultureInfo.CreateSpecificCulture("fr-FR"))))</td>
                   }
                   else
                   {
                    <td>@((Model[i].money.ToString("N0")))</td>
                   }
                   <td>@(Model[i].money.ToString("N0"))</td>
               </tr>
            }*@
            @foreach (var m in Model)
            {
                i++;
                <tr>
                    <td>@i</td>
                    <td>@(m.type)</td>
                    <td>@m.Date.ToString("yyyy-MM-dd")</td>
                    <td>@m.money.ToString("N0")</td>
                </tr>
            }
            @*<tr>
                <td>1</td>
                <td>支出</td>
                <td>2016-01-01</td>
                <td>300</td>
            </tr>
            <tr>
                <td>2</td>
                <td>支出</td>
                <td>2016-01-02</td>
                <td>1,600</td>
            </tr>
            <tr>
                <td>3</td>
                <td>支出</td>
                <td>2016-01-03</td>
                <td>8,00</td>
            </tr>*@
        </table>
    </div>
</div>

